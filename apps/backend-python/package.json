{
  "name": "backend-python",
  "version": "0.0.1",
  "description": "Calendar Todo FastAPI Backend",
  "private": true,
  "scripts": {
    "dev": "uv run --extra dev uvicorn app.main:app --reload --host 0.0.0.0 --port 8001",
    "start": "uv run uvicorn app.main:app --host 0.0.0.0 --port 8001",
    "test": "uv run --extra dev pytest",
    "test:watch": "uv run --extra dev pytest-watch",
    "test:cov": "uv run --extra dev coverage run -m pytest && uv run --extra dev coverage report",
    "lint": "uv run --extra dev flake8 app/ && uv run --extra dev mypy app/",
    "format": "uv run --extra dev black app/ && uv run --extra dev isort app/",
    "build": "echo 'No build step needed for Python'",
    "clean": "find . -type d -name __pycache__ -exec rm -rf {} + || true"
  },
  "dependencies": {},
  "devDependencies": {}
}